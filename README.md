# AdbManager

AdbManager is a program to control your Android devices using `adb` easily, allowing options such as installing apps in any Android supported format (including app bundles), set custom resolutions, browse your files, control your device and make virtual displays using `scrcpy`, and much more.

## Use guide and feature summary coming soon
